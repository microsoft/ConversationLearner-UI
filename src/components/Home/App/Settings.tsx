import * as React from 'react'

const component = (props: any) => (
    <div className="blis-page">
        <h1 className="ms-font-xxl">Settings</h1>
        <p className="ms-font-m-plus">Control your application versions, who has access to it and whether it is public or private...</p>
    </div>
)

export default component